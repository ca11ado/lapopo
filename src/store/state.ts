export const state = {
  user: {
    name: '',
  },
  poker: {
  },
};

export type State = typeof state;

/*
import { User } from '@/api/user';

export interface State {
  user: User;
}

export const state = {
  user: {
    name: '',
  },
};
*/
